import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * pi-keep-going — one-shot `/kg` scheduling + usage-limit auto-resume.
 *
 * Scaffold entry point. Feature implementation is tracked in docs/tasks.md
 * and driven milestone by milestone (M1 core scheduling → M4 publish prep).
 */
export default function (pi: ExtensionAPI): void {
  void pi;
}
